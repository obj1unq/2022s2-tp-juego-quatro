import wollok.game.*
import quatro.*
import configuracion.*

program juego {
	game.title("Juego Base")
	game.boardGround("tablero.png")
	game.width(40)
	game.height(24)
	game.cellSize(35)
//	keyboard.enter().onPressDo({quatro.cambiarSelector()})
	configuracion.configurarPiezas()
	configuracion.agregarVisualizaciones()
	configuracion.ubicarPiezas()
	configuracion.configurarTeclas()
	game.start()
}