import wollok.game.*
import quatro.*

object pepita{
	var property position = game.at(0, 0)
	method image() = "pepita.png"
}
program juego {
	game.title("Juego Base")
	game.boardGround("tablero.png")
	game.width(40)
	game.height(24)
	game.cellSize(35)
//	keyboard.enter().onPressDo({quatro.cambiarSelector()})
	game.addVisualCharacter(quatro)
	game.start()
}