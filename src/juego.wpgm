import wollok.game.*
import configuracion.*

program juego {
	game.title("Quarto")
	game.boardGround("tablero.png")
	game.width(40)
	game.height(24)
	game.cellSize(35)
	
	nivel1.iniciar()
	
	game.start()
}